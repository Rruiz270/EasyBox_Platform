export const createDevelopmentProject = async (req, res) => {
  res.json({ message: 'Create development project - coming soon' });
};

export const getDevelopmentProjects = async (req, res) => {
  res.json({ message: 'Get development projects - coming soon' });
};

export const getDevelopmentProjectById = async (req, res) => {
  res.json({ message: `Get development project ${req.params.id} - coming soon` });
};

export const updateDevelopmentProject = async (req, res) => {
  res.json({ message: `Update development project ${req.params.id} - coming soon` });
};

export const deleteDevelopmentProject = async (req, res) => {
  res.json({ message: `Delete development project ${req.params.id} - coming soon` });
};

export const assignProject = async (req, res) => {
  res.json({ message: `Assign project ${req.params.id} - coming soon` });
};

export const changeProjectStatus = async (req, res) => {
  res.json({ message: `Change project ${req.params.id} status - coming soon` });
};

export const getProjectTimeTracking = async (req, res) => {
  res.json({ message: `Get project ${req.params.id} time tracking - coming soon` });
};