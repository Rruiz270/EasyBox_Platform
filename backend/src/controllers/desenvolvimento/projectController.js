export const createProject = async (req, res) => {
  res.json({ message: 'Create development project - coming soon' });
};

export const getProjects = async (req, res) => {
  res.json({ message: 'Get development projects - coming soon' });
};

export const updateProject = async (req, res) => {
  res.json({ message: `Update project ${req.params.id} - coming soon` });
};

export const deleteProject = async (req, res) => {
  res.json({ message: `Delete project ${req.params.id} - coming soon` });
};