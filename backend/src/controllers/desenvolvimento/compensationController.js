export const createCompensation = async (req, res) => {
  res.json({ message: 'Create compensation - coming soon' });
};

export const getCompensations = async (req, res) => {
  res.json({ message: 'Get compensations - coming soon' });
};

export const updateCompensation = async (req, res) => {
  res.json({ message: `Update compensation ${req.params.id} - coming soon` });
};

export const deleteCompensation = async (req, res) => {
  res.json({ message: `Delete compensation ${req.params.id} - coming soon` });
};

export const applyCompensation = async (req, res) => {
  res.json({ message: 'Apply compensation - coming soon' });
};