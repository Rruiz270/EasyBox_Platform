export const getQuoteParameters = async (req, res) => {
  res.json({ message: 'Get quote parameters - coming soon' });
};

export const createQuoteParameter = async (req, res) => {
  res.json({ message: 'Create quote parameter - coming soon' });
};

export const updateQuoteParameter = async (req, res) => {
  res.json({ message: `Update quote parameter ${req.params.id} - coming soon` });
};

export const deleteQuoteParameter = async (req, res) => {
  res.json({ message: `Delete quote parameter ${req.params.id} - coming soon` });
};