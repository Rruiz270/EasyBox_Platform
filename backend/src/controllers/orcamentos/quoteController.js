export const createQuote = async (req, res) => {
  res.json({ message: 'Create quote - coming soon' });
};

export const getQuotes = async (req, res) => {
  res.json({ message: 'Get quotes - coming soon' });
};

export const getQuoteById = async (req, res) => {
  res.json({ message: `Get quote ${req.params.id} - coming soon` });
};

export const updateQuote = async (req, res) => {
  res.json({ message: `Update quote ${req.params.id} - coming soon` });
};

export const deleteQuote = async (req, res) => {
  res.json({ message: `Delete quote ${req.params.id} - coming soon` });
};

export const duplicateQuote = async (req, res) => {
  res.json({ message: `Duplicate quote ${req.params.id} - coming soon` });
};

export const sendQuoteToClient = async (req, res) => {
  res.json({ message: `Send quote ${req.params.id} to client - coming soon` });
};

export const approveQuote = async (req, res) => {
  res.json({ message: `Approve quote ${req.params.id} - coming soon` });
};

export const rejectQuote = async (req, res) => {
  res.json({ message: `Reject quote ${req.params.id} - coming soon` });
};

export const getQuoteHistory = async (req, res) => {
  res.json({ message: `Get quote ${req.params.id} history - coming soon` });
};