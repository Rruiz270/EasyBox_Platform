export const getQuoteStatuses = async (req, res) => {
  res.json({ message: 'Get quote statuses - coming soon' });
};

export const createQuoteStatus = async (req, res) => {
  res.json({ message: 'Create quote status - coming soon' });
};

export const updateQuoteStatus = async (req, res) => {
  res.json({ message: `Update quote status ${req.params.id} - coming soon` });
};

export const deleteQuoteStatus = async (req, res) => {
  res.json({ message: `Delete quote status ${req.params.id} - coming soon` });
};