export const getSessions = async (req, res) => {
  res.json({ message: 'Get sessions - coming soon' });
};

export const terminateSession = async (req, res) => {
  res.json({ message: 'Terminate session - coming soon' });
};