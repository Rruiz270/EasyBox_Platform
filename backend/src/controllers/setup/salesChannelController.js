export const createSalesChannel = async (req, res) => {
  res.json({ message: 'Create sales channel - coming soon' });
};

export const getSalesChannels = async (req, res) => {
  res.json({ message: 'Get sales channels - coming soon' });
};

export const updateSalesChannel = async (req, res) => {
  res.json({ message: `Update sales channel ${req.params.id} - coming soon` });
};

export const deleteSalesChannel = async (req, res) => {
  res.json({ message: `Delete sales channel ${req.params.id} - coming soon` });
};