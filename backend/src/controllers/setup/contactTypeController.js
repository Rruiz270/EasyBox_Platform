export const createContactType = async (req, res) => {
  res.json({ message: 'Create contact type - coming soon' });
};

export const getContactTypes = async (req, res) => {
  res.json({ message: 'Get contact types - coming soon' });
};

export const updateContactType = async (req, res) => {
  res.json({ message: `Update contact type ${req.params.id} - coming soon` });
};

export const deleteContactType = async (req, res) => {
  res.json({ message: `Delete contact type ${req.params.id} - coming soon` });
};