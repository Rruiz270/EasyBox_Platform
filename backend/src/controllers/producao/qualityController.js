export const createQualityCheck = async (req, res) => {
  res.json({ message: 'Create quality check - coming soon' });
};

export const getQualityChecks = async (req, res) => {
  res.json({ message: 'Get quality checks - coming soon' });
};