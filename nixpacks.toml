[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["echo 'Skipping build phase for Node.js backend'"]

[start]
cmd = "cd backend && npm start"